// Secondary display typeface from Figma (Prata, Google Fonts). Loaded via
// next/font/google in app/layout.tsx; exposed here as the single place
// components reference it, instead of repeating the font-family string.
export const fontPrata = "var(--font-prata), Prata, serif";
